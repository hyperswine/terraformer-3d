# Reference only and for testing

# when using t3d as a library, auto generates a proj structure by copy pasting this with the command `t3dutils --gen-assets-dir`

# when using t3d as an exe, auto generates a proj structure by copy pasting this
